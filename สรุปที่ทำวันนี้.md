# สรุปงานวันนี้ — 1 กันยายน 2026

## งานที่ดำเนินการ

- เชื่อม Angular Price Control กับ Supabase โปรเจกต์ `Med-Site-Audit`
- สร้างตาราง `products`, `price_rules` และ `price_history`
- ทำ Import / Export Excel และรองรับไฟล์ DoctorEase ที่มีชื่อรายงานอยู่ก่อนหัวตาราง
- ทำฟอร์มเพิ่มสินค้า, แก้ไขสินค้า, เลือกหลายรายการ และลบรายการที่เลือก
- เพิ่ม Price History สำหรับเก็บราคาเดิม, ราคาใหม่, เวลา และสาเหตุของการเปลี่ยนราคา
- เพิ่ม Pagination หน้าละ 20 รายการ
- อ่านตัวเลือก Group และ Type จาก DoctorEase แล้วนำมาใช้เป็น dropdown ในฟอร์ม
- เปลี่ยนจากส่วน DF เป็นการตรวจสอบราคาเทียบกับ DoctorEase
- สร้าง DoctorEase API proxy ใน `server.mjs` เพื่อเก็บ API key ไว้ใน `.env` ไม่ให้ปรากฏใน Angular

## ปัญหาที่พบใน Angular และวิธีแก้

### 1. Import Excel สำเร็จในหน้าเว็บ แต่ข้อมูลหายหลังรีเฟรช

**สาเหตุ**

- รอบแรกข้อมูลถูกเก็บใน `localStorage` เท่านั้น
- เมื่อเชื่อม Supabase แล้ว การนำเข้าไฟล์ DoctorEase แบบ bulk upsert ล้มเหลวเพราะมีรหัสสินค้าซ้ำในไฟล์

**สิ่งที่พบ**

- ไฟล์ `DoctorEase - Treatments test.xlsx` มี 2,593 แถว
- มีรหัสสินค้าไม่ซ้ำจริง 2,548 รหัส และมีรหัสซ้ำ 45 แถว

**วิธีแก้**

- ให้ Angular รวมรายการที่มีรหัสซ้ำ โดยใช้ข้อมูลจากแถวล่าสุด
- แบ่งส่งข้อมูลไป Supabase ชุดละ 250 รายการ
- แสดงข้อความผลลัพธ์ชัดเจนเมื่อซิงก์ Supabase สำเร็จหรือไม่สำเร็จ

### 2. Import Excel ไม่พบข้อมูล แม้ไฟล์มีข้อมูลจริง

**สาเหตุ**

- ไฟล์ DoctorEase มีชื่อรายงานอยู่แถวแรก
- หัวคอลัมน์จริง (`Code`, `Name`, `Price`) อยู่แถวที่ 2
- โค้ดเดิมอ่านแถวแรกเป็น header จึงไม่พบคอลัมน์สินค้า

**วิธีแก้**

- ค้นหาแถว header อัตโนมัติจากคอลัมน์ `Code` / `Name` / `Price` หรือชื่อภาษาไทย
- รองรับหัวคอลัมน์ภาษาอังกฤษและภาษาไทย รวมถึงราคาแบบมี comma

### 3. Angular template error จากการเขียน control flow ในบรรทัดเดียว

**อาการ**

- Build ขึ้น error เช่น `Unclosed block "for"`, `Unclosed block "if"` และ `Invalid ICU message`

**สาเหตุ**

- การวาง `@for`, `@if`, `@switch` และ interpolation `{{ ... }}` ซ้อนกันใน HTML บรรทัดเดียว ทำให้ Angular parser แยกขอบเขต block ไม่ถูกต้อง

**วิธีแก้**

- จัดส่วนตารางที่มีเงื่อนไขหลายระดับเป็นหลายบรรทัด
- ใช้ syntax block ของ Angular ให้ชัด เช่น `@if (...) { ... } @else { ... }`
- หลีกเลี่ยง non-null assertion ใน expression ของ template เมื่อไม่จำเป็น

### 4. Angular template ไม่สามารถเรียก `Math` โดยตรง

**อาการ**

- Build error: `Property 'Math' does not exist on type 'PriceControlPageComponent'`

**สาเหตุ**

- Angular template มองชื่อที่ไม่มีใน Component เป็น property ของ Component จึงไม่เข้าถึง global `Math`

**วิธีแก้**

- ย้ายการคำนวณช่วง Pagination ไปไว้ใน `computed()` ของ Component (`pageStart`, `pageEnd`)

### 5. Angular เรียก DoctorEase API โดยตรงไม่ได้

**สาเหตุ**

- API key ต้องถูกส่งด้วย header `X-API-Key`
- ไม่ควรฝัง key ไว้ใน Angular เพราะผู้ใช้เปิดดู source ได้
- การเรียก API จาก browser อาจติด CORS หรือถูก extension บล็อก

**วิธีแก้**

- สร้าง `server.mjs` เป็น local proxy
- Proxy อ่าน key จาก `.env` และเรียก `GET /api/v1/public/services`
- Angular เรียกเฉพาะ `/api/doctorease/services` ผ่าน `proxy.conf.json`
- Proxy cache ราคา DoctorEase 15 นาที เพื่อลดจำนวนคำขอ API

## การใช้งานปัจจุบัน

เปิดสอง Terminal ก่อนใช้งานฟังก์ชันตรวจราคา DoctorEase:

```bash
npm run api
npm start
```

จากนั้นกดปุ่ม `ตรวจราคา DoctorEase` ในหน้าเว็บ ระบบจะแสดง:

- `อัปเดตแล้ว` เมื่อราคาตรงกัน
- `ราคาไม่ตรง` เมื่อราคาบน Supabase ต่างจาก DoctorEase
- `ไม่พบใน DoctorEase` เมื่อไม่พบรหัสสินค้า

## สิ่งที่ควรทำต่อ

- เพิ่มหน้า Login / Register ด้วย Supabase Auth
- เปิด Row Level Security (RLS) และกำหนด policy หลังมีระบบผู้ใช้
- เพิ่มชื่อผู้แก้ไขใน Price History เมื่อมีการล็อกอิน
- พิจารณาย้าย DoctorEase proxy ไปยัง server หรือ Edge Function สำหรับ deployment จริง

---

# สรุปงานเพิ่มเติม — 4 กันยายน 2026

## ผลลัพธ์ที่ยืนยันแล้ว

- หน้า Price Control เชื่อมต่อ Supabase ได้ และโหลดสินค้าครบ **2,549 รายการ** หลังรีเฟรช
- สินค้าทดสอบ `LH300` มีราคา **200 บาท** ทั้ง Price Control และ DoctorEase และแสดงสถานะ `อัปเดตแล้ว`
- เพิ่มตัวกรอง `สถานะราคา` เพื่อดูเฉพาะ `ราคาไม่ตรง` ได้ โดยปัจจุบันพบ 17 รายการ

## ปัญหาและวิธีแก้

### 1. Import Excel แสดงบนเว็บ แต่ไม่บันทึก Supabase

**สาเหตุ**

- ตาราง `products` ใน Supabase มี constraint ของ `df_percent` ที่ขัดกัน: คอลัมน์ห้ามเป็น `null` แต่ constraint ไม่ยอมรับค่า `0` เมื่อ `df_enabled = false`
- ทำให้ bulk upsert จาก Excel ถูกปฏิเสธทั้งชุด และข้อมูลเหลืออยู่เฉพาะใน browser

**วิธีแก้**

- ปรับ schema ผ่าน Supabase SQL Editor: ให้ `df_percent` รับ `null` ได้ และใช้ constraint ที่สอดคล้องกับ model
  - ไม่มี DF: `df_enabled = false`, `df_percent = null`
  - มี DF: `df_enabled = true`, `df_percent` อยู่ระหว่าง 0–100
- ปรับโค้ด import ให้รองรับคอลัมน์ `Have DF` และแจ้งข้อความชัดเจนหาก schema ยังไม่ถูก migrate
- ทดสอบ upsert ด้วยข้อมูล `LH300 / Test / 20` และอ่านข้อมูลกลับจาก Supabase สำเร็จ

### 2. Import สำเร็จ แต่ข้อมูลเกิน 1,000 รายการหายหลังรีเฟรช

**สาเหตุ**

- Supabase Data API จำกัดผลลัพธ์ของคำขอเดียวไว้ที่ 1,000 แถว
- การ import เขียนข้อมูลครบแล้ว แต่ `loadProducts()` อ่านกลับมาเพียง 1,000 แถว จึงดูเหมือนข้อมูลหาย

**วิธีแก้**

- ปรับ `loadProducts()` ให้ดึงแบบ pagination ครั้งละ 1,000 แถวจนหมด
- ยืนยันผ่านหน้าเว็บหลังรีเฟรชว่าแสดงครบ 2,549 รายการ

### 3. DoctorEase เป็น 200 แต่ Price Control แสดง 20

**สาเหตุ**

- Edge Function `doctorease-services` cache ผลจาก DoctorEase นาน 15 นาที
- การตรวจครั้งก่อนจึงยังได้ราคาเก่า 20 แม้ API DoctorEase โดยตรงส่งราคา 200 แล้ว

**วิธีแก้**

- เพิ่มพารามิเตอร์ `?refresh=true` ให้ Edge Function ข้าม cache เมื่อผู้ใช้กดปุ่มตรวจราคา
- ปรับ Price Control ให้ส่งพารามิเตอร์นี้ทุกครั้งที่กด `ตรวจราคา DoctorEase`
- Deploy Edge Function เวอร์ชันใหม่ และยืนยันว่า LH300 แสดง 200 ทั้งสองฝั่ง

## Logic การทำงานปัจจุบัน

### สถานะราคา DoctorEase

- `รอตรวจสอบ`: เพิ่งเปิดหรือรีเฟรชหน้า และยังไม่ได้กดตรวจราคาในรอบนั้น
- `อัปเดตแล้ว`: พบรหัสสินค้าใน DoctorEase และราคาตรงกัน
- `ราคาไม่ตรง`: พบรหัสสินค้าใน DoctorEase แต่ราคาแตกต่างกัน
- `ไม่พบใน DoctorEase`: ดึงข้อมูล DoctorEase สำเร็จ แต่ไม่มีรหัสสินค้านั้น

### ฟิลเตอร์สถานะราคา

- เพิ่ม dropdown ถัดจากฟิลเตอร์กลุ่มสินค้า
- เลือกได้: สถานะทั้งหมด, ราคาไม่ตรง, อัปเดตแล้ว, ไม่พบใน DoctorEase, รอตรวจสอบ
- ฟิลเตอร์ทำงานร่วมกับการค้นหาและฟิลเตอร์กลุ่มสินค้า

### ปุ่ม “ยืนยันการปรับราคา”

- ปุ่มนี้ **ไม่ได้** ยืนยันหรือเขียนราคากลับไป DoctorEase
- ใช้เฉพาะการนำ `กฎปรับราคา` ไปคำนวณและอัปเดตราคาสินค้าทั้งหมดใน Supabase พร้อมบันทึก Price History เป็น `rule_apply`
- การแก้ราคาผ่านปุ่ม `แก้ไข` จะบันทึก Supabase ทันที และบันทึก Price History เป็น `manual_edit`
- หากทีมไม่ได้ใช้ฟีเจอร์กฎปรับราคา ควรพิจารณาซ่อนหรือเอาปุ่มนี้และเมนูกฎปรับราคาออก เพื่อลดความสับสน

## การตรวจสอบ

- `npm run build` ผ่าน
- `npm test` ผ่าน 3/3
- ตรวจในหน้าเว็บจริง: เลือกฟิลเตอร์ `ราคาไม่ตรง` แล้วแสดง 17 รายการ
